{
  "name": "get-ptr-status-api",
  "version": "1.0.0",
  "private": true,
  "description": "API to get status of Property Tax Relief programs",
  "author": "john@innovation.nj.gov",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "package": "rm -rf dist && mkdir dist && cp index.js package.json package-lock.json dist/ && cp -r config dist/ && cd dist && npm ci --omit=dev && zip -r ../lambda.zip . && cd .. && rm -rf dist"
  },
  "dependencies": {
    "oracledb": "^6.0"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "jest": "^29.7.0"
  }
}
