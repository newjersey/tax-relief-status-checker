{
  "name": "tax-relief-status-checker-lambda",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "API to get status of Property Tax Relief programs",
  "author": "john@innovation.nj.gov",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.mjs --format=esm --external:oracledb --external:@aws-sdk/*",
    "test": "vitest run",
    "package": "npm run build && cp package.json ../package-lock.json dist/ && cp -r config dist/ && cd dist && npm ci --omit=dev && zip -r ../lambda.zip . && cd .. && rm -rf dist"
  },
  "dependencies": {
    "@types/oracledb": "6.10.4",
    "oracledb": "6.10.0"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "3.1062.0",
    "@types/aws-lambda": "8.10.145",
    "@types/node": "25.9.1",
    "aws-sdk-client-mock": "4.1.0",
    "esbuild": "0.28.0",
    "typescript": "5.7.3",
    "vitest": "4.1.8"
  }
}
