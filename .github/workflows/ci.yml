name: ci

on:
  pull_request:
    branches:
      - "main"
  push:
    branches:
      - "84-e2e-testing"

jobs:
  prettier:
    uses: ./.github/workflows/prettier.yml
  unit-tests:
    uses: ./.github/workflows/unit_tests.yml
  build:
    uses: ./.github/workflows/build.yml
  e2e-tests:
    uses: ./.github/workflows/ene_to_end_tests.yml
